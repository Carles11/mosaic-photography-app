import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imageHeaderRow: {
    flexDirection: "row",
    alignItems: "center",
    padding: 8,
  },
  mosaicTitle: {
    marginLeft: 10,
    flexShrink: 1,
  },
});
