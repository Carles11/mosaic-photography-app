import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imageHeaderRow: {
    flexDirection: "row",
    alignItems: "center",
    padding: 8,
  },
  mosaicTitle: {
    marginLeft: 10,
    fontWeight: "bold",
    flexShrink: 1,
    fontSize: 14,
  },
});
