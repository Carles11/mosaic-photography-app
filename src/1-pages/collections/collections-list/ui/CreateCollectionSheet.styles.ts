import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  sheetInput: {
    height: 48,
    borderWidth: 1,
    borderRadius: 6,
    marginBottom: 16,
    paddingHorizontal: 12,
  },
  editActions: {
    flexDirection: "row",
    gap: 12,
    marginBottom: 4,
  },
});
