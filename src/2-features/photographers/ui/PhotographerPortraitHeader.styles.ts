import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  // All styles are inline in the component for dynamic layout based on screen size.
  // Add static styles here if you want to refactor later for reuse.
});
