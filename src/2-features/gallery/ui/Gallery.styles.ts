import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 8,
  },
  item: {
    flex: 1,
    margin: 8,
    borderRadius: 8,
    overflow: "hidden",
    elevation: 2,
  },
});
